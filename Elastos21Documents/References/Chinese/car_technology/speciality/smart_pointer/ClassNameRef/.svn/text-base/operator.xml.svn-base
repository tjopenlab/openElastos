<?xml version = "1.0" encoding="GB2312" ?>
<?xml:stylesheet type="text/xsl" href="/xsl/xsl_c/methodmanual.xsl"?>

<manpage>
    <volume> 类智能指针类共有方法 </volume>
    <interface> ClassNameRef </interface>
    <method>operator ==</method>

    <description>
        <para>判断当前类智能指针是否与指定类智能指针指向同一个对象。</para>
    </description>

    <syntax>
        <return_val>BOOL</return_val>
        <parameters>
        <api_param>
            <param_type>const ClassNameRef&amp;</param_type>
            <param_name> cRef </param_name>
            <param_note>
                <para> 指定的类智能指针 </para>
            </param_note>
        </api_param>

        </parameters>
    </syntax>

    <return>
        <para>如果两个类智能指针指向同一个对象，返回TRUE；否则，返回FALSE。</para>
    </return>

    <example><pre><![CDATA[
//g_ClassName是指向ClassName类对象的类智能指针.
ClassNameRef g_ClassName;

void Foo(ClassNameRef& senderRef)
{
    if (!senderRef.IsValid()) { //调用IsValid方法判断类智能指针senderRef的有效性.
        assert(0 && "senderRef is invalid.");
        return;
    }

    if (senderRef == g_ClassName) { //判断senderRef是否与g_ClassName指向同一个对象.
        ……
        return;
    }
    ……
}
]]></pre></example>

</manpage>