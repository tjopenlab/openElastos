<?xml version = "1.0" encoding="GB2312" ?>
    <?xml:stylesheet type="text/xsl" href="/xsl/xsl_c/methodmanual.xsl"?><manpage>

<volume>同步</volume>
<interface>DzReaderWriterLock</interface><method> UpgradeToWriter </method>
    <description>
        <para>将已占有的读者锁升级为写者锁。</para>
    </description>
    <syntax>
        <return_val>void</return_val>
          <parameters>
            <api_param>
                <param_inout>[out]</param_inout>
                <param_type> WaitResult * </param_type>
                <param_name> pResult </param_name>
                <param_note>
                    <para>指向<link url="../../../../../../SoftwareDevelopmentManual/CoreServices/DataStructure/WaitResult.htm">WaitResult</link>枚举类型的指针，用于存放等待结果的变量的地址，不能为空。如果方法调用成功，即调用线程将读者锁升级为写者锁了，则返回WaitResult_OK；如果在等待将读者锁升级为写者锁期间被打断，则返回WaitResult_Interrupted</para>
                </param_note>
            </api_param>
        </parameters>
    </syntax>

    <return>
        <NoReturnValue/>
    </return>

    <remarks>
        <para>本方法用于将已占有的读者锁升级为写者锁（即已经以读者身份占有读写锁对象的调用线程升级为以写者身份占有读写锁对象的线程）。如果调用线程不能升级为以写者身份占有读写锁对象的线程（例如：当读写锁对象被一个或多个读者占有时），调用线程将进入等待状态，直到将已占有的读者锁升级为写者锁为止，才从本方法中成功返回。调用线程在等待将读者锁升级为写者锁期间可以被打断，打断后线程继续已占有的读者锁</para>
        <para>如果调用线程并未以读者身份占有读写锁对象，调用本方法将导致未定义的行为。</para>
    </remarks>

    <requirements>
        <system>
            <system_name>
                Elastos操作系统
            </system_name>
        </system>
    </requirements>

</manpage>

